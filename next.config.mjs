/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Enables React Strict Mode
  images: {
    domains: ["utfs.io"], // Add the domain of your image source
  },
};

export default nextConfig;
